name: Lighthouse CI

on:
  pull_request:
    branches: [main, master]
  push:
    branches: [main, master]
  workflow_dispatch:

jobs:
  lighthouse:
    runs-on: ubuntu-latest
    name: Lighthouse Performance Check
    env:
      GOOGLE_PAGESPEED_API_KEY: ${{ secrets.GOOGLE_PAGESPEED_API_KEY }}
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      
      - name: Install dependencies
        run: npm ci
      
      - name: Run linter
        run: npm run lint
      
      - name: Run type check
        run: npm run type-check
      
      - name: Build production bundle
        run: npm run build
      
      - name: Run Lighthouse CI
        if: env.GOOGLE_PAGESPEED_API_KEY != ''
        env:
          CI: 'true'
          LIGHTHOUSE_MIN_PERFORMANCE: '95'
          LIGHTHOUSE_MAX_LCP: '2500'
        run: npm run lighthouse:ci

      - name: Skip Lighthouse CI
        if: env.GOOGLE_PAGESPEED_API_KEY == ''
        run: echo "Skipping Lighthouse CI because GOOGLE_PAGESPEED_API_KEY is not configured."
      
      - name: Upload Lighthouse results
        if: always() && env.GOOGLE_PAGESPEED_API_KEY != ''
        uses: actions/upload-artifact@v4
        with:
          name: lighthouse-results
          path: docs/lighthouse/lighthouse-ci-*.json
          retention-days: 30
